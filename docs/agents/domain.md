# Domain docs

This repository uses a single-context domain layout. The domain glossary is `CONTEXT.md` at the repository root and architectural decisions live under `docs/adr/`. Create them lazily when useful. Keep `CONTEXT.md` as domain vocabulary rather than an implementation spec. Record ADRs only for consequential, hard-to-reverse trade-offs that future readers would otherwise find surprising.
